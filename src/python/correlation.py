# import pandas as pd
# import numpy as np

# def calc_correlation(source):
#     data = pd.read_json(source).select_dtypes(include="number")
#     return source.corr().replace({np.nan: None}).to_dict()

def func(arr):
    return sum(arr)

